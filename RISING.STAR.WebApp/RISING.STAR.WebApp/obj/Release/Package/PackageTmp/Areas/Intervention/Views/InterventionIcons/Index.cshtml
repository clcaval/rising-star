@model IEnumerable<RISING.STAR.DAL.InterventionIcon>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutInner.cshtml";
}

<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">

    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IconDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IconFileName)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IconDescription)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IconFileName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.InterventionIconGuid }) |
                    @Html.ActionLink("Details", "Details", new { id = item.InterventionIconGuid }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.InterventionIconGuid })
                </td>
            </tr>
        }

    </table>

</div>